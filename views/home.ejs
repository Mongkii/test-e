<!DOCTYPE html>
<head>
  <title>Blog</title>
</head>
<body>
  <a href="/"><h1>Blog</h1></a>
  <form action="/search">
    <label>搜索<input name="q" type="text" value="<%= search %>" /></label>
    <button type="submit">搜索</button>
  </form>
  <ul>
    <% for (var i = 0; i < posts.length; i++) { %>
    <li><a href="/post/<%= posts[i].slug %>"><%= posts[i].title %></a></li>
    <% } %>
  </ul>
</body>
